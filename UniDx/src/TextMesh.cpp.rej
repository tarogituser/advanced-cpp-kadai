diff a/UniDx/src/TextMesh.cpp b/UniDx/src/TextMesh.cpp	(rejected hunks)
@@ -8,6 +8,12 @@ using namespace DirectX;
 
 namespace UniDx {
 
+TextMesh::TextMesh() :
+    text( [this](){ return ToString(u16text); },
+          [this](const u8string& s){ u16text = ToUtf16(s); } )
+{
+}
+
 
 void TextMesh::Awake()
 {
@@ -28,7 +34,7 @@ void TextMesh::render(const Matrix4x4& proj) const
     Vector3 scale = transform->localScale; // 現状はローカルスケールのみ
     Vector2 drawPos(pos.x, pos.y);
     font->getSpriteFont()->DrawString(
-        spriteBatch.get(), ToUtf16(text).c_str(), drawPos, color.XMLoad(), 0.0f, Vector2::zero,
+        spriteBatch.get(), u16text.c_str(), drawPos, color.XMLoad(), 0.0f, Vector2::zero,
         Vector2(scale.x, scale.y) );
 
     spriteBatch->End();
